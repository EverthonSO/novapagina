import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://everthonso.github.io/', // Coloque o link do seu repositório GitHub Pages
  base: '/everthonso.github.io/', // Caminho base do repositório
});
